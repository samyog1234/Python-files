# create a Unicode string
unicode_string = "こんにちは世界"

# print the Unicode string
print(unicode_string)

# get the length of the Unicode string
unicode_string_length = len(unicode_string)
print("Length of Unicode string:", unicode_string_length)

# loop through the Unicode string and print each character
for character in unicode_string:
    print(character)
